# EchoLock Production Environment Configuration
# DO NOT commit this file with real values!

# ============================================
# SERVER CONFIGURATION
# ============================================
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://echolock.xyz

# ============================================
# DATABASE (Railway/Supabase/Neon)
# ============================================
# Get from your database provider
DATABASE_URL=postgresql://user:password@host:port/database

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_RANDOM_64_BYTE_HEX

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SERVICE_ENCRYPTION_KEY=REPLACE_WITH_RANDOM_32_BYTE_HEX

# ============================================
# EMAIL SERVICE (Resend.com)
# ============================================
RESEND_API_KEY=re_your_api_key_here
FROM_EMAIL=EchoLock <noreply@echolock.xyz>

# ============================================
# NOSTR CONFIGURATION
# ============================================
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.nostr.band,wss://nostr.wine

# ============================================
# CORS
# ============================================
CORS_ORIGINS=https://echolock.xyz,https://www.echolock.xyz

# ============================================
# MONITORING (Optional)
# ============================================
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
